Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0
razorpay==1.4.1
requests==2.31.0
psycopg2-binary==2.9.10

# Minimal dependencies for lightweight operation
# Removed: Pillow, numpy, nltk, textblob, email-validator, secure-smtplib
# These can be added back when needed

# Heavy AI dependencies removed for Render free tier to prevent overload
# torch>=2.0.0
# transformers>=4.21.0
# accelerate>=0.20.0
# peft>=0.4.0