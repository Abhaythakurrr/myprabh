Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0
razorpay==1.4.1
requests==2.31.0
psycopg2-binary==2.9.10

# Ultra-lightweight memory-based AI - zero heavy dependencies!
# Perfect for Render free tier (512MB RAM, 0.1 CPU)
# Memory usage: <50MB vs 500MB+ with heavy ML models

# Removed heavy dependencies:
# - Pillow, numpy, nltk, textblob, email-validator, secure-smtplib
# - torch, transformers, accelerate, peft (not needed for memory-based AI)

# The app now uses pure Python with regex and basic string processing
# for natural, memory-aware AI responses that prove the concept beautifully!